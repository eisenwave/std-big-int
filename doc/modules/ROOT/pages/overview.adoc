// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
// SPDX-License-Identifier: BSL-1.0

[#overview]
:idprefix: overview_

= `beman::big_int`: A reference implementation for https://isocpp.org/files/papers/D4444R0.html[P4444]

[#overview_introduction]
== Introduction

pass:[C++] currently provides no portable support for integers wider than 64 bits.
This is one of the oldest and most widely recognized gaps in the language, which authors have attempted to fill several times, and all proposed a `std::integer` class with infinite precision.
Each of these attempts failed — not because there was a lack of interest or motivation, but because the task turned out to be too great of a challenge.

`std::big_int` is to `long long` what `std::string` is to `char[N]`: sure, a fixed size is sufficient for some use cases, but comes with a constant threat of overflow and undefined behavior, and that threat is easily eliminated at some acceptable runtime cost.

[#overview_motivation]
== Motivation

This repository contains our reference implementation of `std::big_int` as `beman::big_int`.
It is designed to be both portable and performant, with all features specified in https://isocpp.org/files/papers/D4444R0.html[P4444].

[#overview_supported_compilers]
== Supported Compilers

This repository requires a minimum of pass:[C++23], and is tested on the following compilers:

* GCC 13 and later
* Clang 17 and later
* MSVC 19.43 and later

The optional pass:[C++] named module needs newer toolchains than the headers
do -- GCC 15 or later, Clang 20 or later, or a current MSVC -- plus CMake 3.30
or later and a generator that can scan module dependencies. AppleClang is not
supported for the module build. It is off by default, so this applies only if
you turn it on; see xref:modules.adoc[pass:[C++] Named Modules].
